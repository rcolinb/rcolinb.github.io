exclude:
  - _incantations.json
  - _scripts
  - README.md
  # The cardiac simulator publishes as a single built file; its build inputs
  # stay in the repository but are not served.
  - cardiac/src
  - cardiac/build.py
  - cardiac/CONTRACT.md
  - cardiac/README.md
